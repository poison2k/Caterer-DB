@using PagedList.Mvc
@using Caterer_DB.Models
@model FullFilterCatererViewModel

@{
    ViewBag.Title = "Index";
}

@using (Html.BeginForm())
{
    <br />
    <br />
    <br />
    <div class="container">
        <div class="row">
            <div id="CatererSeite" class="col-md-4 col-md-offset-4 text-center">
                <h1>Caterer</h1>
            </div>
        </div>
        <br />

        <div class="row">
            <div class="col-md-12">
                <div class="well well-lg-weiss" style="border-color: rgb(211, 211, 211);">

                    <div class="row">
                        <div class="col-md-12">
                            <div class="form-group">
                                <label class="control-label">Firmenname</label>
                                @Html.Editor("name", new { htmlAttributes = new { @class = "form-control", id = "name" } })
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label class="control-label">Postleitzahl</label>
                                @Html.Editor("plz", new { htmlAttributes = new { @class = "form-control", id = "plz" } })
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label class="control-label">Umkreis</label>
                                @Html.Editor("umkreis", new { htmlAttributes = new { @class = "form-control", id = "umkreis" } })
                            </div>
                        </div>
                    </div>
                    <br />
                    <br />
                    <div class="row">
                        <div class="col-md-6">
                            <label class="control-label">Frage</label>
                            @Html.Editor("umkreis", new { htmlAttributes = new { @class = "form-control", id = "umkreis" } })
                        </div>
                        <div class="col-md-4">
                            <label class="control-label">Antwort</label>
                            @Html.Editor("umkreis", new { htmlAttributes = new { @class = "form-control", id = "umkreis" } })
                        </div>
                        <div class="col-md-2 text-right">
                            <a class="btn btn-default" style="margin-top: 27px;"><i class="glyphicon glyphicon-plus"></i></a>
                            <a class="btn btn-default" style="margin-top: 27px;"><i class="glyphicon glyphicon-trash"></i></a>
                        </div>
                    </div>
                    <br />
                    <div class="row">
                        <div class="col-md-3">
                            <button type="submit" class="btn btn-default btn-block">Filter anwenden</button>
                        </div>
                        <div class="col-md-3">
                            <button type="submit" class="btn btn-default btn-block"> Filter zurücksetzen</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <br />
        <div class="row">
            <div class="col-md-12">
                <div class="row">
                    <ul class="list-unstyled list-inline col-md-12" style="margin-left: -5px;">
                        <li>
                            <a id="btnMarkierungenErstellen" href="@Url.Action("CreateCaterer", "Benutzer")" na name="btnErstellen" type="submit" class="btn btn-success">
                                <i class="glyphicon glyphicon-plus"></i> Neuer Caterer
                            </a>
                        </li>
                    </ul>
                </div>
                <div style="margin-top: 15px"></div>
                <div class="row" id="BenutzerIndexPartial">
                    <div class="col-md-12">
                        <div class="panel panel-default">
                            <div class="panel-heading"></div>
                            <table class="table table-striped">
                                <tr>
                                    <th>
                                        @if (ViewBag.Sortierrung == "Firmenname")
                                        {
                                            <button type="submit" class="link-button" href="@Url.Action("IndexCaterer", "Benutzer", new { Sortierrung = "Firmenname_desc" })"> @Html.DisplayNameFor(model => model.ResultListCaterer.FirstOrDefault().Firmenname) <b class="caret"></b> </button>
                                        }
                                        else if (ViewBag.Sortierrung == "Firmenname_desc")
                                        {
                                            <button type="submit" class="link-button" href="@Url.Action("IndexCaterer", "Benutzer", new { Sortierrung  = "Firmenname"})"> @Html.DisplayNameFor(model => model.ResultListCaterer.FirstOrDefault().Firmenname) <b class="caret caret-reversed"></b> </button>
                                        }
                                        else
                                        {
                                            <button type="submit" class="link-button" href="@Url.Action("IndexCaterer", "Benutzer", new { Sortierrung = "Firmenname" })"> @Html.DisplayNameFor(model => model.ResultListCaterer.FirstOrDefault().Firmenname)  </button>
                                        }
                                    </th>
                                    <th>
                                        @if (ViewBag.Sortierrung == "Telefon")
                                        {
                                            <button type="submit"   class="link-button" href="@Url.Action("IndexCaterer", "Benutzer", new { Sortierrung = "Telefon_desc"})">
                                                @Html.DisplayNameFor(model => model.ResultListCaterer.FirstOrDefault().Telefon) <b class="caret"></b> </button>
                                        }
                                        else if (ViewBag.Sortierrung == "Telefon_desc")
                                        {
                                            <button type="submit"   class="link-button" href="@Url.Action("IndexCaterer", "Benutzer", new { Sortierrung  = "Telefon"})">
                                                @Html.DisplayNameFor(model => model.ResultListCaterer.FirstOrDefault().Telefon) <b class="caret caret-reversed"></b> </button>
                                        }
                                        else
                                        {
                                            <button type="submit"   class="link-button" href="@Url.Action("IndexCaterer", "Benutzer", new { Sortierrung  = "Telefon"})">
                                                @Html.DisplayNameFor(model => model.ResultListCaterer.FirstOrDefault().Telefon)  </button>
                                        }
</th>
                                    <th>
                                        @if (ViewBag.Sortierrung == "Postleitzahl")
                                        {
                                            <a href="@Url.Action("IndexCaterer", "Benutzer", new { Sortierrung = "Postleitzahl_desc"})">@Html.DisplayNameFor(model => model.ResultListCaterer.FirstOrDefault().Postleitzahl) <b class="caret"></b> </a>
                                        }
                                        else if (ViewBag.Sortierrung == "Postleitzahl_desc")
                                        {
                                            <a href="@Url.Action("IndexCaterer", "Benutzer", new { Sortierrung  = "Postleitzahl"})"> @Html.DisplayNameFor(model => model.ResultListCaterer.FirstOrDefault().Postleitzahl) <b class="caret caret-reversed"></b> </a>
                                        }
                                        else
                                        {
                                            <a href="@Url.Action("IndexCaterer", "Benutzer", new { Sortierrung  = "Postleitzahl"})"> @Html.DisplayNameFor(model => model.ResultListCaterer.FirstOrDefault().Postleitzahl)  </a>
                                        }
                                    </th>
                                    <th>
                                        @if (ViewBag.Sortierrung == "Ort")
                                        {
                                            <a href="@Url.Action("IndexCaterer", "Benutzer", new { Sortierrung = "Ort_desc"})">@Html.DisplayNameFor(model => model.ResultListCaterer.FirstOrDefault().Ort) <b class="caret"></b> </a>
                                        }
                                        else if (ViewBag.Sortierrung == "Ort_desc")
                                        {
                                            <a href="@Url.Action("IndexCaterer", "Benutzer", new { Sortierrung  = "Ort"})"> @Html.DisplayNameFor(model => model.ResultListCaterer.FirstOrDefault().Ort) <b class="caret caret-reversed"></b> </a>
                                        }
                                        else
                                        {
                                            <a href="@Url.Action("IndexCaterer", "Benutzer", new { Sortierrung  = "Ort"})"> @Html.DisplayNameFor(model => model.ResultListCaterer.FirstOrDefault().Ort)  </a>
                                        }
                                    </th>

                                    <th></th>
                                </tr>

                                @foreach (var item in Model.ResultListCaterer.Entitäten)
                                {
                                    <tr>
                                        <td>@Html.DisplayFor(modelItem => item.Firmenname)</td>
                                        <td>@Html.DisplayFor(modelItem => item.Telefon)</td>
                                        <td>@Html.DisplayFor(modelItem => item.Postleitzahl)</td>
                                        <td>@Html.DisplayFor(modelItem => item.Ort)</td>

                                        <td align="center">
                                            <a class="btn btn-default" href="@Url.Action("DetailsCaterer", "Benutzer", new {id = item.BenutzerId})" title="Details"><i class="glyphicon glyphicon-search"></i></a>
                                            <a class="btn btn-default" href="@Url.Action("EditCaterer", "Benutzer", new { id = item.BenutzerId })" title="Bearbeiten"><i class="glyphicon glyphicon-pencil"></i></a>
                                        </td>
                                    </tr>
                                }
                            </table>
                        </div>

                        @if (Model.ResultListCaterer.TotalItemCount > Model.ResultListCaterer.PageSize)
                        {
                            var seitenAnzahl = Model.ResultListCaterer.TotalItemCount / Model.ResultListCaterer.PageSize;
                            if (Model.ResultListCaterer.TotalItemCount % Model.ResultListCaterer.PageSize > 0)
                            {
                                seitenAnzahl++;
                            }
                            <input id="seitenAnzahl" type="hidden" value="@seitenAnzahl" />
                            <div controller="/Benutzer/GetListPartial/" target="BenutzerIndexPartial">
                                @Html.PagedListPager(Model.ResultListCaterer, aktuelleSeite => Url.Action("IndexCaterer", new { aktuelleSeite, Model.ResultListCaterer, Sortierrung = ViewBag.Sortierrung }))
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>

}