@model IEnumerable<DataAccess.Model.RechteGruppe>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Bezeichnung)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Löschbar)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Bezeichnung)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Löschbar)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RechteVerwaltungsGruppeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RechteVerwaltungsGruppeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RechteVerwaltungsGruppeId })
        </td>
    </tr>
}
</table>