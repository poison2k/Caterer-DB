@using Caterer_DB.Models
@model DetailsFrageViewModel

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Frage</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Bezeichnung)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Bezeichnung)
        </dd>

        @foreach (var item in Model.Antworten)
        {
            <dt>
                @item.AntwortId
            </dt>

            <dd>
                @Html.DisplayFor(modelitem => item.Bezeichnung)
            </dd>
        }

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.FrageId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
